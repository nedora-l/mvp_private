import NextAuth from "next-auth"
import { authConfig } from "./auth.config"

/**
 * NextAuth configuration with auth.js
 * @see https://authjs.dev
 */
export const { auth, signIn, signOut } = NextAuth(authConfig)
